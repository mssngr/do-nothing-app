# Migration `20200517160255-remove-tokens-from-db`

This migration has been generated by Gabriel Konkle at 5/17/2020, 4:02:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" DROP COLUMN "accessToken",
DROP COLUMN "refreshToken";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200517154725-ids-as-strings..20200517160255-remove-tokens-from-db
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -15,11 +15,9 @@
   lastName       String
   password       String
   avatarUrl      String?
   roles          String[]
-  activationCode String?
-  refreshToken   String?
-  accessToken    String?
+  activationCode String?  @default(uuid())
   isActivated    Boolean  @default(false)
   createdAt      DateTime @default(now())
   updatedAt      DateTime @updatedAt
   refreshedAt    DateTime @default(now())
```


