# Migration `20200518085011-updates`

This migration has been generated by Gabriel Konkle at 5/18/2020, 8:50:11 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" DROP COLUMN "avatarUrl",
DROP COLUMN "salt";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200517170720-remove-roles..20200518085011-updates
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -12,11 +12,9 @@
   email       String   @unique
   phone       String
   firstName   String
   lastName    String
-  salt        String
   hash        String
-  avatarUrl   String?
   isActivated Boolean  @default(false)
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
   refreshedAt DateTime @default(now())
```


