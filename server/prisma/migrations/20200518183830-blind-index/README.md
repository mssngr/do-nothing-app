# Migration `20200518183830-blind-index`

This migration has been generated by Gabriel Konkle at 5/18/2020, 6:38:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."User.email"

ALTER TABLE "public"."User" ADD COLUMN "emailIndex" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200518085011-updates..20200518183830-blind-index
--- datamodel.dml
+++ datamodel.dml
@@ -1,16 +1,17 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
 }
 model User {
   id          String   @default(uuid()) @id
-  email       String   @unique
+  email       String
+  emailIndex  String
   phone       String
   firstName   String
   lastName    String
   hash        String
```


